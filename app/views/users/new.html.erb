
<div class="newuserform">
  <h1>Signup New User</h1>
  <%= form_for @user do |f| %>

  <div class="errors">
    <% if @user.errors.any? %>
      <div id="error_explanation">
        <h2>
          <%= pluralize(@user.errors.count, "error") %>
        </h2>
        <ul>
          <% @user.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
  </div>

    <p>
      <%= f.label :name %><br />
      <%= f.text_field :name, :autofocus => true, class: "userbox" %>
    </p>

    <p>
      <%= f.label :username %><br />
      <%= f.text_field :username, class: "userbox" %>
    </p>

    <p>
      <%= f.label :email %><br>
      <%= f.email_field :email, class: "userbox" %>
    </p>

    <p>
      <%= f.label :password %><br>
      <%= f.password_field :password, class: "userbox" %>
    </p>
    <%= f.submit "Create Profile", class: "userbutton" %>
  <% end %>

</div>
